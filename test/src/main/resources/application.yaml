spring.vivid:
  rest:
    polling:
      interval: 5s
    base-url: http://localhost:8080
  application-id: test
  environment: Dev
  streams:
    - sse

server:
  port: 8808
logging.level:
    com.vivid: TRACE
